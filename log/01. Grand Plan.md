Bismillah.

# Grand Plan: Dzikr & Dua

## 1. Data Ingestion & State
Ingest mburoja-api JSON. Audio stored locally. No DB.
- **Code:** `src/lib/data.ts`, `src/store/audio.ts`
- **Step:**
  - [x] Fetch `invocations.json`. Define `Invocation`, `Chapter` types.
  - [x] Serve audio locally from `public/audios/`.
  - [x] Setup Zustand `useAudioStore`. Store queue, nowPlaying, playback state.
- **Verify:**
  - [x] `getChapters()` return data.
  - [x] Store state update on play.

## 2. Design System & Theming
Apply Terra Design System. Rooted Warmth.
- **Code:** `tailwind.config.js`, `src/index.css`
- **Step:**
  - [x] Map colors: `primary: #4a7c59`, `background: #faf6f0`.
  - [x] Set fonts: Literata, Nunito Sans.
  - [x] Update shadcn radius (12px).
  - [x] Rule: `flex gap-*` not `space-y-*`. Use semantic colors.
- **Verify:**
  - [x] Check colors. Contrast ≥4.5:1.
  - [x] Touch targets ≥44x44px.

## 3. UI Components & Layouts
Build UI. Follow UX flows. Use Shadcn.
- **Code:** `src/components/`, `src/routes/__root.tsx`
- **Step:**
  - [x] Mount `<audio>` in `__root.tsx`. Prevent unmount on navigate.
  - [x] Build `AudioPlayer`, `QueueSidebar`, `Search`. Use `Card`, `Slider`, `Button`.
  - [x] Apply `ssr-hydration-safety` (TanStack rule).
  - [x] Use `cn()` for classes. Apply `file-separation`.
- **Verify:**
  - [x] Audio play during route change.
  - [x] Keyboard nav work. ARIA labels present.

## 4. Semantic Search
Client-side vector search. No DB.
- **Code:** `src/lib/search.ts`, `src/components/Search.tsx`
- **Step:**
  - [x] Setup local search index (Orama / Transformers.js).
  - [x] Index English meaning and Arabic.
  - [x] Connect to UI Search input.
- **Verify:**
  - [x] Search by concept (e.g., "forgiveness") find related Dua.
  - [x] Search fast, no server call.

## 5. Routing & URL State
Sync queue to URL. Smart auto-play.
- **Code:** `src/routes/index.tsx` (Home), `src/routes/play.tsx` (Player), `src/lib/url.ts`
- **Step:**
  - [x] Build Intro Homepage at `/`.
  - [x] Build Player at `/play`.
  - [x] Parse `?queue=` from URL. Validate IDs. Compress (`lz-string`).
  - [x] Time-based auto-play: Morning visit -> auto queue Morning Dhikr (Chapter 27). Afternoon visit -> auto queue Afternoon Dhikr (Chapter 28).
- **Verify:**
  - [x] URL share load exact queue.
  - [x] Auto-play match user local time.

## 6. UX Testing
Validate design.
- **Code:** `PLAYBOOKS/usability.md`
- **Step:**
  - Test 5+ users.
  - Check empty states, error states.
- **Verify:**
  - Task completion > 70%.
  - Fix issues.

Alhamdulillah.
