Bismillah.

# Grand Plan: Dzikr & Dua

## 1. Data Ingestion & State
Ingest mburoja-api JSON. Audio stored locally. No DB.
- **Code:** `src/lib/data.ts`, `src/store/audio.ts`
- **Step:**
  - Fetch `invocations.json`. Define `Invocation`, `Chapter` types.
  - Serve audio locally from `public/audios/`.
  - Setup Zustand `useAudioStore`. Store queue, nowPlaying, playback state.
- **Verify:**
  - `getChapters()` return data.
  - Store state update on play.

## 2. Design System & Theming
Apply Terra Design System. Rooted Warmth.
- **Code:** `tailwind.config.js`, `src/index.css`
- **Step:**
  - Map colors: `primary: #4a7c59`, `background: #faf6f0`.
  - Set fonts: Literata, Nunito Sans.
  - Update shadcn radius (12px).
  - Rule: `flex gap-*` not `space-y-*`. Use semantic colors.
- **Verify:**
  - Check colors. Contrast ≥4.5:1.
  - Touch targets ≥44x44px.

## 3. UI Components & Layouts
Build UI. Follow UX flows. Use Shadcn.
- **Code:** `src/components/`, `src/routes/__root.tsx`
- **Step:**
  - Mount `<audio>` in `__root.tsx`. Prevent unmount on navigate.
  - Build `AudioPlayer`, `QueueSidebar`, `Search`. Use `Card`, `Slider`, `Button`.
  - Apply `ssr-hydration-safety` (TanStack rule).
  - Use `cn()` for classes. Apply `file-separation`.
- **Verify:**
  - Audio play during route change.
  - Keyboard nav work. ARIA labels present.

## 4. Semantic Search
Client-side vector search. No DB.
- **Code:** `src/lib/search.ts`, `src/components/Search.tsx`
- **Step:**
  - Setup local search index (Orama / Transformers.js).
  - Index English meaning and Arabic.
  - Connect to UI Search input.
- **Verify:**
  - Search by concept (e.g., "forgiveness") find related Dua.
  - Search fast, no server call.

## 5. Routing & URL State
Sync queue to URL. Smart auto-play.
- **Code:** `src/routes/index.tsx` (Home), `src/routes/play.tsx` (Player), `src/lib/url.ts`
- **Step:**
  - Build Intro Homepage at `/`.
  - Build Player at `/play`.
  - Parse `?queue=` from URL. Validate IDs. Compress (`lz-string`).
  - Time-based auto-play: Morning visit -> auto queue Morning Dhikr (Chapter 27). Afternoon visit -> auto queue Afternoon Dhikr (Chapter 28).
- **Verify:**
  - URL share load exact queue.
  - Auto-play match user local time.

## 6. UX Testing
Validate design.
- **Code:** `PLAYBOOKS/usability.md`
- **Step:**
  - Test 5+ users.
  - Check empty states, error states.
- **Verify:**
  - Task completion > 70%.
  - Fix issues.

Alhamdulillah.
