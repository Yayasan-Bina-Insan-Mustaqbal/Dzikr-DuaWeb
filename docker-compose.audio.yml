version: '3.8'
services:
  audio-processor:
    image: python:3.9-slim
    volumes:
      - .:/app
    working_dir: /app
    command: >
      sh -c "apt-get update && 
             apt-get install -y ffmpeg && 
             python3 scripts/cut_evening_rodja_v2.py"
